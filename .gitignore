# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Pixi (ambiente local — NÃO versionar)
.pixi/
# pixi.lock É versionado (reprodutibilidade)

# Variáveis de ambiente (contêm senhas)
.env

# Dados de cliente (nunca vão pro Git)
dados/*.xlsx
dados/*.csv
dados/*.db
dados/*.db-shm
dados/*.db-wal
!dados/.gitkeep

# Sistema operacional
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
